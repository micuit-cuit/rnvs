let toggleCTP = false;
function confgToPicture() {
    toggleCTP = !toggleCTP;
    if (toggleCTP) {
        displayPIP(true);
    } else {
        displayPIP(false);
    }
}
function displayPIP(toggle) {
    console.log('not implemented');
}